name: Build QuestCraft Nexa Vivecraft Phase 1.4

on:
  workflow_dispatch:
  push:
    branches:
      - questcraft-nexa-androidstudio
    paths:
      - 'questcraft_nexa/vivecraft/**'
      - 'questcraft_nexa/patch_vivecraft.py'
      - '.github/workflows/build-questcraft-nexa-vivecraft.yml'

permissions:
  contents: read

jobs:
  build-vivecraft:
    runs-on: ubuntu-24.04
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4

      - uses: actions/checkout@v4
        with:
          repository: QuestCraftPlusPlus/VivecraftMod
          ref: openxr-1.20.4
          path: upstream/vivecraft
          fetch-depth: 1

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Inject NexaVR Phase 1.4
        shell: bash
        run: |
          set -euo pipefail
          ROOT="$GITHUB_WORKSPACE/upstream/vivecraft"
          SRC="$GITHUB_WORKSPACE/questcraft_nexa/vivecraft/nexavr"
          DST="$ROOT/common/src/main/java/org/vivecraft/client_vr/provider/nexavr"
          test -d "$SRC"
          mkdir -p "$DST"
          cp -f "$SRC"/*.java "$DST/"
          python3 questcraft_nexa/patch_vivecraft.py "$ROOT"
          test -f "$DST/NexaVR.java"
          test -f "$DST/NexaTrackingReceiver.java"
          test -f "$DST/NexaVRStereoRenderer.java"
          grep -Fq 'NEXA' "$ROOT/common/src/main/java/org/vivecraft/client_vr/settings/VRSettings.java"
          grep -Fq 'MirrorMode.DUAL' "$ROOT/common/src/main/java/org/vivecraft/client_vr/settings/VRSettings.java"
          grep -Fq 'case NEXA -> new NexaVR(instance, dh);' "$ROOT/common/src/main/java/org/vivecraft/client_vr/VRState.java"

      - name: Build Fabric JAR
        working-directory: upstream/vivecraft
        shell: bash
        run: |
          set -euo pipefail
          chmod +x ./gradlew
          ./gradlew :fabric:build --stacktrace --no-daemon --warning-mode all | tee "$GITHUB_WORKSPACE/vivecraft-build.log"

      - name: Collect JAR
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p out/vivecraft
          find upstream/vivecraft/fabric/build/libs -maxdepth 1 -type f -name '*.jar' -print | tee out/vivecraft/all-jars.txt
          JAR="$(find upstream/vivecraft/fabric/build/libs -maxdepth 1 -type f -name '*.jar' ! -name '*sources*' ! -name '*dev*' ! -name '*shadow*' -print -quit)"
          test -n "$JAR"
          test -s "$JAR"
          cp "$JAR" out/vivecraft/vivecraft-nexa-1.20.4-fabric.jar
          sha256sum out/vivecraft/vivecraft-nexa-1.20.4-fabric.jar > out/vivecraft/vivecraft-nexa-1.20.4-fabric.jar.sha256

      - name: Upload JAR
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: QuestCraft-Nexa-Vivecraft-Phase14
          path: |
            out/vivecraft/**
            vivecraft-build.log
          if-no-files-found: warn
          retention-days: 14
